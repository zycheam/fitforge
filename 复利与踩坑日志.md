# 复利与踩坑日志

> 每次任务完成后更新：记录新踩的坑、可复用方法、值得总结的经验。

---

## 2026-08-03：4个Skill信息写入AGENTS.md

### 完成内容
- 将4个核心Skill（UI UX Pro Max / Impeccable / React Bits / Karpathy Guidelines）的环境、配置、网络需求、调用方式写入 `AGENTS.md`
- 明确了每次创建网页的Skill调用流程

### 环境速查
| Skill | 环境 | API Key | 网络 | 本项目适用 |
|-------|------|---------|------|-----------|
| UI UX Pro Max | Python 3.x | 无 | 本地 | ✅ |
| Impeccable | Node 18+ | 无 | 本地 | ✅ |
| React Bits | Node 18+, React | 无 | 本地 | ❌（纯HTML） |
| Karpathy Guidelines | 无 | 无 | 本地 | ✅ |

---

## 历史经验

### 项目部署
- GitHub Pages 在线地址: https://zycheam.github.io/fitforge/
- 落地页: https://zycheam.github.io/fitforge/fitforge-landing.html

### 数据
- free-exercise-db: 873条练习，已全部汉化
- 数据文件: `exercises_data.js` / `exercises_data.json`

### 分享功能
- 使用 UTF-8 → Base64 编码替代废弃的 `btoa(unescape(...))`
- 分享链接微信扫码后可打开

### 常见问题
- 纯HTML项目无法使用React Bits（那是React组件库）
- Skill是Codex内部设计工具，在对话中描述需求即可自动调用
- 不需要在HTML中引入任何Skill代码

---

## 2026-08-03：训练计划添加视频教程功能

### 完成内容
- 训练计划中每个动作旁增加"📺 视频"按钮，点击弹出视频搜索模态框
- 视频模态框支持：
  - B站API自动搜索（调用 `api.bilibili.com` 搜索接口，展示前6条视频结果）
  - 降级方案：API失败时显示手动搜索按钮（B站搜索 / 抖音搜索）
  - ESC键关闭视频模态框
- 分享独立页面中每个动作添加"📺 观看教程"链接（跳转B站搜索）
- 修复了PowerShell字符串转义导致的引号问题

### 技术要点
- B站API地址：`https://api.bilibili.com/x/web-interface/search/type?search_type=video&keyword=健身+动作名`
- B站搜索URL：`https://search.bilibili.com/all?keyword=健身+动作名&order=click`
- 抖音搜索URL：`https://www.douyin.com/search/动作名?type=general`
- API可能有跨域限制，已实现降级方案

### 踩坑记录
- PowerShell中处理JavaScript模板字面量 `${ex.id}` 时需注意转义
- `buildStandalonePage` 中CSS字符串拼接需保持格式一致，新加行也要带 `' +` 前缀
---

## 2026-08-03：视频教程 + SHARE_BASE_URL修复 + 部署上线

### 修复内容
- **SHARE_BASE_URL 未定义**：分享链接此前一直指向 `undefined#share=...`，现已定义为 `window.location.origin + window.location.pathname`，本地和线上均正常工作
- **视频教程重构**：B站API全面封锁外部跨域调用，改为直接打开B站/抖音搜索页面，100%可靠
  - 点击"📺 视频"弹出模态框
  - "在B站搜索"按钮 → `search.bilibili.com/all?keyword=健身教程+动作名`
  - "在抖音搜索"按钮 → `www.douyin.com/search/动作名`
  - 两平台均在新标签页打开，不被拦截
- **分享独立页面**：每个动作添加"📺 观看教程"链接

### 踩坑记录
- B站API (`api.bilibili.com`) 有反爬/WAF保护，返回412 Precondition Failed，curl/Invoke-WebRequest均被拦截
- 浏览器端fetch调用同样受CORS限制
- 结论：直接跳转B站搜索页是唯一可靠方案

### 部署
- 已推送到 GitHub Pages：https://zycheam.github.io/fitforge/
- 手机扫码可打开，分享链接正常
---

## 2026-08-03：修复Pages构建失败（无效子模块）

### 问题
- GitHub Pages构建在Checkout步骤失败，原因是仓库中包含无效的git子模块 `andrej-karpathy-skills-repo`（无`.gitmodules`映射）
- 同时清理了已删除的 `free-exercise-db-main` 冗余副本（30个文件约23K行）

### 修复
- `git rm --cached andrej-karpathy-skills-repo` 移除子模块引用
- `git rm -r --cached free-exercise-db-main` 移除冗余仓库副本
- Pages构建恢复成功

### 关键教训
- GitHub Pages使用Jekyll构建时，仓库必须干净——无效子模块会导致Checkout失败
- Pages构建状态可通过API查看：`api.github.com/repos/{owner}/{repo}/actions/runs`
- CDN缓存约10分钟（Cache-Control: max-age=600），部署后需等待刷新
---

## 2026-08-04 — ThoughtLab 风格改造 FitForge Landing Page

**任务**: 将 itforge-landing.html 重新设计为 Refero Design 上 ThoughtLab 风格

**数据来源**: 通过 https://styles.refero.design/api/styles/82d52a5f-b1bb-4a69-91a3-15a7eb8bbe99 获取完整设计规范 JSON

**改造要点**:
- 色板替换: 从橙/绿改为 Void(#000)+Ash(#ccc)+Frost(#fff)+Graphite(#4c4c4c)+Crimson(#fc1c46)
- 字体: Barlow → Space Grotesk (sui 替代)
- 卡片: 移除所有背景填充、边框、阴影 — 透明漂浮在黑色画布上
- 按钮: 全部 pill 形状 (9999px radius)，Crimson 仅用于主 CTA
- 排版: 标题大写，91px/72px/27px 层级，正文 #cccccc 非纯白
- 边距: 桌面 126px，section gap 108px

**踩坑**:
- Refero 页面是 Next.js SPA，静态 HTML 无设计数据；需调用其 API /api/styles/{id} 获取 JSON
- Impeccable CLI 脚本路径嵌套过深 (.codex/skills/impeccable/.claude/skills/impeccable/scripts/)，手动审查替代

**可复用方法**:
- 从 Refero 提取设计规范: curl https://styles.refero.design/api/styles/{UUID} 返回完整 designSystem JSON
- 设计规范自查脚本: 用 PowerShell 正则检查色板/阴影/字体/行高/边距是否符合目标规范
---

## 2026-08-04 — Dylanbrouwer 极简建筑风格全局改造

**任务**: 将 fitforge-landing.html 和 index.html 从 ThoughtLab 风格切换为 Dylanbrouwer 极简建筑风格

**数据来源**: https://styles.refero.design/style/b1e82907-d1cf-46cd-8ae7-3561c5b15fd0 → API JSON

**改造要点**:
- 色板: #161616 (主背景) / #201d1d (表面) / #252225 (高层) / #ff6436 (暖橙强调)
- 字体: IBM Plex Mono (UI/等宽) + Inter (正文/标题) + PingFang SC / Microsoft YaHei (中文回退)
- 卡片: 无背景、无阴影，仅 1px rgba(255,255,255,0.06) 边框，悬停变表面色
- 按钮: Pill 造型 9999px，等宽大写，accent 背景
- 排版: 超大标题 (96px/line-height 0.9)，Inter 900 字重

**踩坑**:
- 落地页 CSS token 污染: --bg-card: #f8f8f8 / --bg-light: #f1f1f1 是亮色，与暗色主题冲突。虽未实际使用，但会误导维护，已修正为暗色。
- index.html 中文字体回退缺失: --font-mono 和 --font-sans 未包含 PingFang SC / Microsoft YaHei，中文内容会退到系统默认字体，已补充。
- Impeccable 子代理不可用: 降级为单上下文手动审查，已按 SKILL.md 规范在报告中标注 DEGRADED。

**可复用方法**:
- CSS 批量替换: 用 Python 脚本通过 <style>…</style> 边界定位并整体替换 CSS 块，比手动编辑快且不易出错。
- 字体回退检查: 所有含中文内容的页面需确认 font-family 包含 'PingFang SC', 'Microsoft YaHei' 等中文回退。
- Token 清理: 风格切换后应搜索所有 CSS 变量定义，移除或修正不匹配的旧 token。
- 落地页与主页 CSS 一致性: 两页应共享相同的 :root token 定义，减少维护成本。

**Impeccable 审查结论**:
- 落地页: 移动端导航缺失 (nav-links display:none 无替代)，其余良好
- 主页: 侧边栏移动端 max-height:45vh 可能不足，其余良好
- 两页都已正确处理 prefers-reduced-motion 和 focus-visible 无障碍

